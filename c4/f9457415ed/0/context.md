# Session Context

## User Prompts

### Prompt 1

Update create-project.jsx.

After form inputs, calculate grid using cols = ceil(sqrt(num_plots)) and rows = ceil(num_plots/cols).
Display grid size and individual plot dimensions below form.
Keep logic simple.

